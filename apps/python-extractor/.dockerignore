# Version-control + editor noise
.git
.gitignore
.github
.vscode
.idea

# Python build + cache artifacts
__pycache__
*.pyc
*.pyo
*.pyd
.Python
*.egg-info
*.egg
build
dist
.venv
venv
env

# Test + tooling artifacts
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov

# (README.md IS needed — pyproject.toml's [project].readme field points
# at it and hatchling's wheel build fails without it. Keep it in the
# build context.)
