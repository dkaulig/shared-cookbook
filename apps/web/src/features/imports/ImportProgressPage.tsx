/**
 * Placeholder — the real implementation lands in P2-7 step 4.
 * Kept here so App.tsx's route table references a real module.
 */
export function ImportProgressPage() {
  return <div>Import wird vorbereitet …</div>
}
