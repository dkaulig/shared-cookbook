/**
 * Response from the API /api/health endpoint.
 * Hand-written for now; will be generated from the .NET OpenAPI spec in a later slice.
 */
export interface HealthResponse {
  status: string
  timestamp: string
}
